services:
  - type: web
    name: vuta-resolver
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    envVars:
      - key: PORT
        value: 10000
      - key: RESOLVER_API_KEY
        generateValue: true
